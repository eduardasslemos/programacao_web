type Tarefa = {
    descricao: string;
    prioridade: number;
    concluida: boolean;
};

/*function executarTarefas (objetos: Tarefa[], callback: (param: string, number, number) => void, intervalo: number = 1000): void{

    callback(descricao, indice, quantidade);
}*/